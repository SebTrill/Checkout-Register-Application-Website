<UserControl x:Class="FriedPiper.PointOfSale.PiperPlatterDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FriedPiper.PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="leftPieString" Text="{Binding Path=LeftPie}" Margin="20,0,0,0"/>
        <TextBlock Grid.Row="1" Name="rightPieString" Text="{Binding Path=RightPie}" Margin="20,0,0,0"/>
        <TextBlock Grid.Row="2" Name="leftIceCreamString" Text="{Binding Path=LeftIceCream}" Margin="20,0,0,0"/>
        <TextBlock Grid.Row="3" Name="rightIceCreamString" Text="{Binding Path=RightIceCream}" Margin="20,0,0,0"/>
    </Grid>
</UserControl>
